event_inherited();

if(x != target_x && y != target_y){
	//set player object invisible
	//create player run object
	//when player arrives at target point, destroy player run object
	//set player object visible again
}